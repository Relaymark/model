{
  "name": "relaymark.model",
  "description": "Relaymark model factory",
  "main": "./dist/relaymark.model.js",
  "keywords": [
    "relaymark",
    "model"
  ],
  "author": "Relaymark",
  "dependencies": {
    "ng-file-upload": "^12.0.4"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "rimraf": "^2.5.2"
  },
  "license": "ISC",
  "optionalDependencies": {},
  "publishConfig": {
    "registry": "http://npm.relaymark.com"
  },
  "scripts": {
    "build": "rimraf dist && babel src --presets babel-preset-es2015 --out-dir dist",
    "clean": "rimraf dist"
  },
  "version": "1.1.66",
  "repository": {
    "type": "git",
    "url": "https://github.com/Relaymark/Relaymark.model.git"
  }
}
